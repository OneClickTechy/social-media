import React from 'react'

const About = () => {
  return (
    <section>
        <h1 className="text-6xl font-bold">About</h1>
        <p className="mt-4 text-2xl">
          This is a simple blog built with React, Tailwind CSS, and Vite.
        </p>
        <p className="mt-4 text-2xl">
          It uses a JSON file as a database and React Router for client-side routing.
        </p>
    </section>
  )
}

export default About